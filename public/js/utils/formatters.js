// Utilitaires de formatage
export const Formatters = {
    formatDate(date) {
        // Logique de formatage de date
    },
    formatSalary(amount) {
        // Logique de formatage de salaire
    }
};